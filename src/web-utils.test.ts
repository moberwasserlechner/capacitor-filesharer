describe('data conversion', () => {
    it('convert to byteArray', async () => {
        // WebUtils.toByteArray(oneDriveOptions).then(webOptions => {
        //     expect(webOptions).not.toBeNull();
        // });
    });

});


